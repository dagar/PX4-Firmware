menuconfig MODULES_VTOL_CONTROL_ALLOCATOR
	bool "vtol_control_allocator"
	default n
	---help---
		Enable support for vtol_control_allocator

menuconfig USER_VTOL_CONTROL_ALLOCATOR
	bool "vtol_control_allocator running as userspace module"
	default y
	depends on BOARD_PROTECTED && MODULES_VTOL_CONTROL_ALLOCATOR
	---help---
		Put control_allocator in userspace memory
